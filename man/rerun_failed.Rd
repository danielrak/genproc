% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rerun_failed.R
\name{rerun_failed}
\alias{rerun_failed}
\title{Re-run only the cases that failed}
\usage{
rerun_failed(
  r0,
  f,
  parallel = NULL,
  nonblocking = NULL,
  track_inputs = TRUE,
  input_cols = NULL,
  skip_input_cols = NULL
)
}
\arguments{
\item{r0}{A \code{genproc_result} produced by \code{\link[=genproc]{genproc()}}. Its
\verb{$reproducibility$mask_snapshot} provides the original mask.}

\item{f}{A function. Typically the same function passed to the
original \code{genproc()} call. The result object does not store
\code{f}, so it must be supplied here. If the previous failures
were caused by a bug in \code{f}, pass the corrected version.}

\item{parallel, nonblocking, track_inputs, input_cols, skip_input_cols}{Forwarded to \code{\link[=genproc]{genproc()}} for the re-run.}
}
\value{
A new \code{genproc_result} covering only the failed cases.
Its \code{case_id}s are local to the subset (re-numbered starting at
\code{case_0001}); the link back to the original \code{r0} is via the
matching rows of \code{r0$reproducibility$mask_snapshot}. If \code{r0}
has zero failed cases, the function returns \code{NULL} with a
message — there is nothing to re-run.
}
\description{
Filters the original mask of \code{r0} down to the cases for which
\code{success == FALSE} and re-runs \code{genproc()} on that subset. Useful
when a transient external problem caused some cases to fail and
the user has fixed the cause: rather than re-running the whole
mask, only the failed cases are refreshed.
}
\examples{
r0 <- genproc(
  f = function(x) if (x \%\% 2 == 0) stop("even") else x,
  mask = data.frame(x = 1:6)
)
# 3 cases failed (the even ones). After fixing f, retry only those:
\dontrun{
  r1 <- rerun_failed(r0, f = function(x) abs(x))
  r1$log
}

}
\seealso{
\code{\link[=rerun_affected]{rerun_affected()}}, \code{\link[=errors]{errors()}}, \code{\link[=summary.genproc_result]{summary.genproc_result()}}
}
