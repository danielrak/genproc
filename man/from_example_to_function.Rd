% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_example_to_function.R
\name{from_example_to_function}
\alias{from_example_to_function}
\title{Transform an example expression into a parameterized function}
\usage{
from_example_to_function(expr, env = parent.frame())
}
\arguments{
\item{expr}{An expression of length 1, typically created with
\code{\link[=expression]{expression()}} or \code{\link[=quote]{quote()}} wrapped in \code{\link[=as.expression]{as.expression()}}.}

\item{env}{The environment in which to look up symbols. Symbols found
in this environment that are \strong{not} functions will be turned into
parameters. Defaults to the caller's environment.}
}
\value{
A function whose body is the rewritten expression and whose
formals are the detected parameters with their default values.
The function's environment is set to \code{env}.
}
\description{
Takes a concrete R expression (e.g. a data processing script that
works on one specific case) and returns a function where every
external value (strings, variables from the environment that are not
functions) has been replaced by a named parameter with the original
value as default.
}
\details{
This is the first step in the genproc workflow: the user writes a
working example, and \code{from_example_to_function()} extracts a
reusable, parameterized version of it.
\subsection{What gets parameterized}{
\itemize{
\item \strong{String literals}: every string in the expression becomes a
parameter (e.g. \code{"output.csv"} -> \code{param_1} with default
\code{"output.csv"}).
\item \strong{Non-function symbols}: if a symbol exists in \code{env} and its value
is not a function, it becomes a parameter.
}
}

\subsection{What is left unchanged}{
\itemize{
\item \strong{Locally bound symbols}: variables created by assignments inside
the expression (e.g. \code{result <- ...}) are never parameterized.
\item \strong{Function names}: the head of a call (e.g. \code{read.csv} in
\code{read.csv(path)}) is never parameterized.
\item \strong{Functions in the environment}: symbols whose value is a function
are assumed to be part of the program structure, not data.
\item \strong{Numeric, logical, NULL, and other non-character atomic values}.
}
}

\subsection{Deduplication}{

The same value produces the same parameter. If \code{"output.csv"} appears
twice, both occurrences map to the same \code{param_N}.
}
}
\examples{
# --- Basic usage ---
# `input_path` exists in this environment; "output.csv" is a
# string literal. Both become parameters of the resulting function,
# with their original values as defaults.
input_path <- "/data/input.csv"

expr <- expression({
  df <- read.csv(input_path)
  write.csv(df, "output.csv")
})

fn <- from_example_to_function(expr)
formals(fn)

# --- Local bindings are protected ---
# `x` is assigned inside the block, so it is NOT parameterized
# even though x = 42 exists in the calling environment.
x <- 42
expr2 <- expression({
  x <- 1
  y <- x + 1
})
fn2 <- from_example_to_function(expr2)
formals(fn2)

}
