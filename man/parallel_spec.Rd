% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_spec.R
\name{parallel_spec}
\alias{parallel_spec}
\title{Specify a parallel execution strategy for genproc()}
\usage{
parallel_spec(
  workers = NULL,
  strategy = NULL,
  chunk_size = NULL,
  seed = TRUE,
  packages = NULL,
  globals = TRUE
)
}
\arguments{
\item{workers}{Integer >= 1, or \code{NULL}. Number of workers to use.
Ignored when \code{strategy = "sequential"}. If \code{NULL}, the current
\code{future::plan()} decides.}

\item{strategy}{Character, or \code{NULL}. One of \code{"sequential"},
\code{"multisession"}, \code{"multicore"}, \code{"cluster"}. If \code{NULL}
(default), the current \code{future::plan()} is used unchanged. If
specified, genproc temporarily sets the corresponding plan for
the run and restores the previous plan on exit.}

\item{chunk_size}{Integer >= 1, or \code{NULL}. Number of iteration
cases bundled per future. Larger values reduce scheduling
overhead at the cost of load-balance granularity. \code{NULL}
delegates to \code{future.apply}'s default heuristic.}

\item{seed}{Controls reproducible random-number generation
across workers. Passed to \code{future.apply::future_lapply()}'s
\code{future.seed} argument. Default \code{TRUE} derives independent
L'Ecuyer-CMRG streams from a random master seed. A single
integer fixes the master seed. \code{FALSE} disables reproducible
RNG and is not recommended unless the user function is known
to be RNG-free.}

\item{packages}{Character vector, or \code{NULL}. Extra packages to
attach on each worker before running the user function.
\pkg{genproc} itself is attached automatically for every
strategy other than \code{"sequential"}.}

\item{globals}{Logical or character. Forwarded to
\code{future.apply::future_lapply()}'s \code{future.globals}. Default
\code{TRUE} enables automatic detection, which is correct in almost
all cases. Set to a character vector only to override detection.}
}
\value{
A list of class \code{"genproc_parallel_spec"} with the
validated, normalized fields.
}
\description{
Returns a configuration object to pass as the \code{parallel} argument
of \code{\link[=genproc]{genproc()}}. The object describes \emph{how} to parallelize; the
actual execution is carried out by \code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}
on top of the backend selected by \code{\link[future:plan]{future::plan()}}.
}
\section{Choosing a strategy}{

\itemize{
\item \code{"sequential"}: runs in the current process, no workers.
Exercises the parallel code path without the overhead; useful
for deterministic testing.
\item \code{"multisession"}: portable (works on Windows), launches R
subprocesses via \pkg{parallelly}. The default recommendation
for most workloads.
\item \code{"multicore"}: forks the current process (Unix/macOS only,
\strong{not available on Windows} and not reliable inside RStudio).
Faster startup than multisession but loses portability.
\item \code{"cluster"}: explicit cluster of workers, possibly on other
machines. For large-scale batch execution.
}

For most users, leaving \code{strategy = NULL} and calling
\code{future::plan()} once at the top of the session is the cleanest
setup.
}

\section{RNG reproducibility}{


With \code{seed = TRUE}, each case receives an independent
L'Ecuyer-CMRG stream derived from a random master seed. Same
master seed -> identical results regardless of worker count or
chunking. To pin the master seed, pass an integer
(\code{seed = 42L}).
}

\examples{
# Use whatever plan the caller has set
spec <- parallel_spec()

# One-off parallel call with 4 workers, reproducible RNG
spec <- parallel_spec(workers = 4, strategy = "multisession",
                      seed = 42L)

# Exercise the parallel code path deterministically in a test
spec <- parallel_spec(strategy = "sequential")

}
