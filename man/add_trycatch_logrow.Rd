% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_trycatch_logrow.R
\name{add_trycatch_logrow}
\alias{add_trycatch_logrow}
\title{Wrap a function with error handling and a structured log row}
\usage{
add_trycatch_logrow(f)
}
\arguments{
\item{f}{A function to wrap.}
}
\value{
A function with the same formals as \code{f}, whose return value
is a one-row data.frame with columns: all original arguments,
\code{success} (logical), \code{error_message} (character or \code{NA}),
\code{traceback} (character or \code{NA}), \code{duration_secs} (numeric).
}
\description{
Takes a function and returns a modified version that:
\itemize{
\item always returns a one-row data.frame (the "log row")
\item captures errors without stopping, recording the error message
and the \strong{real} traceback (call stack at the point of failure)
\item records wall-clock execution time
}
}
\details{
\subsection{Why not plain tryCatch?}{

\code{tryCatch()} unwinds the call stack before entering the error handler.
This means \code{sys.calls()} inside a \code{tryCatch} error handler returns
the handler's own stack, not the stack that led to the error.

This function uses \code{withCallingHandlers()} (which fires while the
original stack is still intact) to capture the traceback, then lets
the error propagate to an outer \code{tryCatch()} for control flow.
}

\subsection{Log row structure}{

The returned data.frame always has one row. Columns:
\itemize{
\item One column per formal argument of \code{f}, with the value passed
\item \code{success}: \code{TRUE} if the body executed without error
\item \code{error_message}: the error's \code{conditionMessage()}, or \code{NA}
\item \code{traceback}: the formatted call stack at the error, or \code{NA}
\item \code{duration_secs}: wall-clock seconds elapsed
}
}
}
\examples{
safe_sqrt <- add_trycatch_logrow(function(x) sqrt(x))
safe_sqrt(4)    # success = TRUE, duration_secs > 0
safe_sqrt("a")  # success = FALSE, error_message filled, traceback filled

}
