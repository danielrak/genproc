% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff_inputs.R
\name{diff_inputs}
\alias{diff_inputs}
\alias{print.genproc_input_diff}
\title{Compare input file fingerprints between two genproc runs}
\usage{
diff_inputs(r0, r1)

\method{print}{genproc_input_diff}(x, ...)
}
\arguments{
\item{r0, r1}{Two \code{genproc_result} objects. By convention, \code{r0} is
the earlier run and \code{r1} the later one, but the function is
symmetric with respect to \code{changed} / \code{unchanged}. The labels
\code{removed} (present in \code{r0}, absent in \code{r1}) and \code{added} (the
opposite) follow the asymmetric convention.}

\item{x}{A \code{genproc_input_diff} object.}

\item{...}{Ignored (present for S3 method consistency).}
}
\value{
An object of class \code{genproc_input_diff} (a named list)
with components:
\describe{
\item{method}{Character, e.g. \code{"stat"}.}
\item{changed}{A data.frame with columns \code{path},
\code{size_before}, \code{size_after}, \code{mtime_before}, \code{mtime_after}.
One row per file whose size or mtime differs.}
\item{unchanged}{Character vector of paths whose size and
mtime are identical in both runs.}
\item{removed}{Character vector of paths present in \code{r0}'s
snapshot but absent in \code{r1}'s.}
\item{added}{Character vector of paths present in \code{r1}'s
snapshot but absent in \code{r0}'s.}
\item{cases_affected}{A data.frame with columns \code{case_id},
\code{path}, \code{column}, \code{change_type} (one of \code{"changed"},
\code{"removed"}, \code{"added"}). One row per (case, input column)
impacted by the diff. Pass to \code{\link[=rerun_affected]{rerun_affected()}} to re-run
only the impacted cases.}
}
}
\description{
Takes two \link[=genproc]{genproc_result} objects produced by
\code{\link[=genproc]{genproc()}} (the same function over the same mask, run at two
different times) and reports which referenced input files have
changed since the first run.
}
\details{
Files are matched by canonical absolute path. The \code{method} field
must agree between the two runs.
}
\examples{
# Two runs of the same procedure, with one input file rewritten
# in between. `diff_inputs()` reports the drift.
src <- file.path(tempdir(), "diff-inputs-demo")
dir.create(src, showWarnings = FALSE, recursive = TRUE)
write.csv(head(iris), file.path(src, "a.csv"), row.names = FALSE)

mask <- data.frame(
  path = file.path(src, "a.csv"),
  stringsAsFactors = FALSE
)
read_one <- function(path) nrow(read.csv(path))

r0 <- genproc(read_one, mask)

# Rewrite the file with strictly more rows: size changes.
write.csv(iris, file.path(src, "a.csv"), row.names = FALSE)

r1 <- genproc(read_one, mask)
diff_inputs(r0, r1)

}
