% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rename_function_params.R
\name{rename_function_params}
\alias{rename_function_params}
\title{Rename the parameters of a function}
\usage{
rename_function_params(f, mapping)
}
\arguments{
\item{f}{A function whose parameters should be renamed.}

\item{mapping}{A named character vector. Names are the \strong{current}
parameter names, values are the \strong{new} names.
Example: \code{c(param_1 = "input_path", param_2 = "output_path")}.}
}
\value{
A function with renamed formals and body.
}
\description{
Takes a function and a name mapping, and returns a new function where
both the formals and all symbol references in the body have been
renamed according to the mapping. This is typically used after
\code{\link[=from_example_to_function]{from_example_to_function()}} to replace generated names like
\code{param_1}, \code{param_2} with meaningful names.
}
\details{
\subsection{Validation}{

The function checks that:
\itemize{
\item All names in \code{mapping} actually exist as formals of \code{f}
\item New names are unique (no duplicates)
\item New names do not collide with parameters not being renamed
}
}

\subsection{Limitation}{

If the body contains a nested function definition whose formals
shadow a parameter being renamed, the shadowed references in that
inner body will still be renamed. This is unlikely in practice
(parameters from \code{\link[=from_example_to_function]{from_example_to_function()}} are named \code{param_N})
but is noted here for completeness.
}
}
\examples{
fn <- function(param_1 = "in.csv", param_2 = "out.csv") {
  df <- read.csv(param_1)
  write.csv(df, param_2)
}

fn2 <- rename_function_params(fn, c(
  param_1 = "input_path",
  param_2 = "output_path"
))
# fn2 now has formals input_path and output_path,
# and the body uses those names.

}
