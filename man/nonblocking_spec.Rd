% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonblocking_spec.R
\name{nonblocking_spec}
\alias{nonblocking_spec}
\title{Specify a non-blocking execution strategy for genproc()}
\usage{
nonblocking_spec(strategy = "multisession", packages = NULL, globals = TRUE)
}
\arguments{
\item{strategy}{Character, or \code{NULL}. One of \code{"sequential"},
\code{"multisession"}, \code{"multicore"}, \code{"cluster"}. Default
\code{"multisession"}. Unlike \code{\link[=parallel_spec]{parallel_spec()}}, the default is
not \code{NULL}: a function named "non-blocking" must not silently
block because the current \code{future::plan()} is sequential. Pass
\code{strategy = NULL} explicitly to defer to the caller's plan.
\code{"sequential"} is accepted mainly for deterministic testing —
it exercises the code path but does \emph{not} actually free the
console.}

\item{packages}{Character vector, or \code{NULL}. Extra packages to
attach on the background worker before running. \pkg{genproc}
itself is attached automatically for every strategy other than
\code{"sequential"}.}

\item{globals}{Logical or character. Forwarded to
\code{future::future()}'s \code{globals} argument. Default \code{TRUE} enables
automatic detection.}
}
\value{
A list of class \code{"genproc_nonblocking_spec"} with the
validated, normalized fields.
}
\description{
Returns a configuration object to pass as the \code{nonblocking}
argument of \code{\link[=genproc]{genproc()}}. When supplied, \code{genproc()} returns
immediately with a \code{genproc_result} of status \code{"running"} while
the actual work continues in a background future. Use \code{\link[=status]{status()}}
to poll the state and \code{\link[=await]{await()}} to block until completion.
}
\section{Composition with parallel_spec()}{


\code{nonblocking_spec()} and \code{\link[=parallel_spec]{parallel_spec()}} are orthogonal and can
be combined. The non-blocking layer launches \emph{one} outer future;
inside it, the parallel layer dispatches cases via
\pkg{future.apply}. With both strategies set to \code{"multisession"},
\pkg{future} resolves the inner layer as \code{"sequential"} by default
(see \code{future::plan()} nesting rules) unless the caller installs an
explicit nested plan via \code{future::plan(list(...))}.
}

\examples{
# Launch in the background, keep the console
\dontrun{
  spec <- nonblocking_spec()
  job <- genproc(f = slow_fn, mask = mask, nonblocking = spec)
  status(job)           # "running"
  job <- await(job)     # blocks until done
  job$log
}

# Deterministic test: exercise the code path without real async
spec <- nonblocking_spec(strategy = "sequential")

}
\seealso{
\code{\link[=parallel_spec]{parallel_spec()}}, \code{\link[=status]{status()}}, \code{\link[=await]{await()}}
}
