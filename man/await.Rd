% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/status_await.R
\name{await}
\alias{await}
\alias{await.genproc_result}
\title{Block until a non-blocking genproc run has resolved}
\usage{
await(x, ...)

\method{await}{genproc_result}(x, ...)
}
\arguments{
\item{x}{An object. Methods exist for \code{genproc_result}.}

\item{...}{Unused, for future extensions.}
}
\value{
A \code{genproc_result} with \code{status != "running"}.
}
\description{
\code{await()} blocks until the background future of a non-blocking
\code{\link[=genproc]{genproc()}} run has resolved, then returns a \code{genproc_result}
with \code{log}, \code{n_success}, \code{n_error}, \code{duration_total_secs}, and
\code{status} populated. If the wrapper future itself crashed (a rare
case — user errors inside individual cases are caught by the
logging layer and are \emph{not} wrapper crashes), the returned
object has \code{status = "error"} and a populated \code{error_message}.
}
\details{
\code{await()} is idempotent: calling it on a result that has already
been materialized (or was synchronous to begin with) returns it
unchanged.
}
\seealso{
\code{\link[=status]{status()}}, \code{\link[=nonblocking_spec]{nonblocking_spec()}}
}
