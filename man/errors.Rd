% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect_helpers.R
\name{errors}
\alias{errors}
\alias{errors.genproc_result}
\title{Subset a genproc result to its failed cases}
\usage{
errors(x, ...)

\method{errors}{genproc_result}(x, ...)
}
\arguments{
\item{x}{A \code{genproc_result} produced by \code{\link[=genproc]{genproc()}}.}

\item{...}{Unused, for future extensions.}
}
\value{
A data.frame with one row per failed case. Empty
data.frame (with the same columns) if there are no failures.
Returns \code{NULL} (with a message) if the run is non-blocking and
has not been materialized yet.
}
\description{
Returns the rows of \code{result$log} corresponding to cases where
\code{success == FALSE}. The columns are exactly those of
\code{result$log} (case_id, mask parameters, success, error_message,
traceback, duration_secs).
}
\examples{
result <- genproc(
  f = function(x) if (x \%\% 2 == 0) x / 0 else x,
  mask = data.frame(x = 1:6)
)
errors(result)[, c("case_id", "x", "error_message")]

}
\seealso{
\code{\link[=rerun_failed]{rerun_failed()}}, \code{\link[=summary.genproc_result]{summary.genproc_result()}}
}
