% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rerun_affected.R
\name{rerun_affected}
\alias{rerun_affected}
\title{Re-run only the cases impacted by an input diff}
\usage{
rerun_affected(
  r0,
  diff,
  f,
  parallel = NULL,
  nonblocking = NULL,
  track_inputs = TRUE,
  input_cols = NULL,
  skip_input_cols = NULL
)
}
\arguments{
\item{r0}{A \code{genproc_result} produced by \code{\link[=genproc]{genproc()}}. Its
\verb{$reproducibility$mask_snapshot} provides the original mask;
it must contain \code{track_inputs = TRUE} (the default).}

\item{diff}{A \code{genproc_input_diff} produced by \code{\link[=diff_inputs]{diff_inputs()}}.}

\item{f}{A function. Typically the same function passed to the
original \code{genproc()} call. The result object does not store
\code{f}, so it must be supplied here.}

\item{parallel, nonblocking, track_inputs, input_cols, skip_input_cols}{Forwarded to \code{\link[=genproc]{genproc()}} for the re-run. By default, these
inherit a sensible behaviour: \code{track_inputs = TRUE} (so the
re-run is itself comparable), the other arguments default to
\code{NULL} (sequential, blocking, automatic input tracking).}
}
\value{
A new \code{genproc_result} covering only the affected cases.
Its \code{case_id}s are local to the subset (re-numbered starting at
\code{case_0001}); the link back to the original \code{r0} is via the
matching rows of \code{r0$reproducibility$mask_snapshot}. If \code{diff}
reports no affected cases, the function returns \code{NULL} with a
message — there is nothing to re-run.
}
\description{
Filters the original mask of \code{r0} down to the cases that
referenced inputs reported as changed, removed, or added by
\code{\link[=diff_inputs]{diff_inputs()}}, and re-runs \code{genproc()} on that subset.
}
\details{
This is the actionable end of the reproducibility layer: when an
upstream file silently drifts, you do not need to re-run the
whole mask. \code{rerun_affected()} produces a smaller run that
refreshes only the impacted outputs.
}
\examples{
\dontrun{
  r0 <- genproc(my_fn, my_mask)
  # ... time passes, some upstream files change ...
  r1 <- genproc(my_fn, my_mask)

  d <- diff_inputs(r0, r1)
  # d$cases_affected lists the case_ids whose inputs drifted.

  refreshed <- rerun_affected(r0, d, f = my_fn)
  refreshed$log
}

}
\seealso{
\code{\link[=diff_inputs]{diff_inputs()}}, \code{\link[=genproc]{genproc()}}
}
