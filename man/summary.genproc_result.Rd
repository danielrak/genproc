% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect_helpers.R
\name{summary.genproc_result}
\alias{summary.genproc_result}
\title{Summarise a genproc result}
\usage{
\method{summary}{genproc_result}(object, top_errors = 10L, ...)
}
\arguments{
\item{object}{A \code{genproc_result} produced by \code{\link[=genproc]{genproc()}}.}

\item{top_errors}{Integer. Maximum number of distinct error
messages to include in the summary, ranked by occurrence.
Default 10.}

\item{...}{Unused, for future extensions.}
}
\value{
An object of class \code{genproc_result_summary} (a list)
with components:
\describe{
\item{materialized}{Logical. \code{FALSE} if the run is
non-blocking and has not been collected via \code{\link[=await]{await()}}.
In that case the other fields are \code{NA}.}
\item{status}{Character, mirrors \code{result$status}.}
\item{n_cases}{Integer.}
\item{n_success, n_error}{Integers.}
\item{success_rate}{Numeric in \link{0, 1}.}
\item{duration_total_secs}{Numeric, wall-clock total.}
\item{duration_stats}{List with \code{total}, \code{mean}, \code{max},
and \code{slowest_case_id}. \code{NULL} if no per-case durations.}
\item{top_errors}{data.frame with columns \code{error_message} and
\code{count}, sorted by count descending. Trimmed to
\code{top_errors} rows.}
}
}
\description{
Produces a compact digest of the run: status, success rate,
duration stats, and the top recurring error messages. Useful
on runs with a lot of cases where the raw log is too noisy to
eyeball.
}
\examples{
result <- genproc(
  f = function(x) {
    if (x \%\% 2 == 0) stop("even")
    if (x \%\% 3 == 0) stop("multiple of three")
    x
  },
  mask = data.frame(x = 1:12)
)
summary(result)

}
\seealso{
\code{\link[=errors]{errors()}}, \code{\link[=rerun_failed]{rerun_failed()}}
}
